window.SIDEBAR_ITEMS = {"struct":[["RecycleRef","a reference to a managed T instance that allows mutation.  When the reference is destroyed then the managed T instance will be recycled back into the [Recycler]. This reference is not thread safe but shareable thread safe references can be created by calling [RecycleRef::to_shared]"],["Recycler","A buffer that manages the recycling of dropped items that were pulled from this buffer."],["RecyclerBuilder","Uses the builder pattern to pre allocate T instances and build the [Recycler]"],["SharedRecycleRef","A thread safe recycle reference that allows read access to the underlying item. Once all instances of the shared recycle reference that contain the same item are drop then the item is recycled back into the recycler buffer. "]]};